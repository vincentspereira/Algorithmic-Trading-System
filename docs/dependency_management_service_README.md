# Dependency Management System

This directory contains the dependency management framework for the Algorithmic Trading System.

## Structure

- `tiers/` - Contains the dependencies organized by tiers
- `monitoring/` - GitHub Actions workflows for monitoring dependencies
- `notifications/` - Notification system configurations
- `testing/` - Docker-based testing environments
- `dashboards/` - Health dashboard configurations
- `security/` - Security scanning configurations